// About Controller - Handles about section interactions
const aboutController = {
  getEducationList() {
    // Could fetch from API in a real app
    return;
  },

  handleEducationClick(institution) {
    console.log('Education clicked:', institution);
  },

  getHighlights() {
    // Could filter or sort highlights
    return;
  }
};

export default aboutController;
